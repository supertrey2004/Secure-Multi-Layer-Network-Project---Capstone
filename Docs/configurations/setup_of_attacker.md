# Attacker

1. Installing OS
    - Select Install Ubuntu
    - Leave all settings default
    - When it asks for network insert IP address (10.0.104.30)
    - When it comes to user name user attacker, password 123 (As this system will not be part of the security test a simple password makes it easier)

2. Updating
    > sudo apt update  
    > sudo apt upgrade  
    - Run these commands to update the system password is necessary

3. Confirm system operations and update

4. Installing softwares